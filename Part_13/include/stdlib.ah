#ifndef _STDLIB_AH
# define _STDLIB_AH
#include <stddef.ah>

void *malloc(size_t size);
void free(void *ptr);
void exit(int64 status);
void *calloc(size_t nmemb, size_t size);
void *realloc(void *ptr, size_t size);

int system(char *command);

flt64 strtod(char *nptr, char **endptr);
flt32 strtof(char *nptr, char **endptr);

uint64 strtoull(char *nptr, char **endptr, int base);


#endif
